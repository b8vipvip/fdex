# FDEX chat2api streaming adapter

FDEX consumes chat2api through its own server-side OpenAI-compatible gateway. The Android client requests SSE streaming and keeps API credentials on the FDEX server.

The Android chat UI now uses a ChatGPT-inspired layout: user messages stay in compact right-aligned bubbles, AI replies render as full-width Markdown content, and employee actions live in the top app bar.
