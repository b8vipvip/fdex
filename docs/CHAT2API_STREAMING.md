# FDEX chat2api streaming adapter

FDEX consumes chat2api through its own server-side OpenAI-compatible gateway. The Android client requests SSE streaming and keeps API credentials on the FDEX server.
