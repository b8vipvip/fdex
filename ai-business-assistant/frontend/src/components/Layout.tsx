import type { ReactNode } from 'react';
import { Link, NavLink } from 'react-router-dom';
const tabs=[['💬','消息','/messages'],['📁','项目','/projects'],['🧭','发现','/discover'],['👤','我的','/me']];
export default function Layout({children,title}:{children:ReactNode;title?:string}){return <div className="min-h-screen bg-slate-100"><aside className="fixed inset-y-0 left-0 hidden w-60 border-r bg-white p-5 lg:block"><Link to="/messages" className="text-xl font-bold">AI 虚拟公司</Link><p className="mt-2 text-sm text-slate-500">你的随身 AI 团队</p><nav className="mt-8 space-y-2">{tabs.map(([icon,label,to])=><NavLink key={to} to={to} className={({isActive})=>`flex min-h-12 items-center gap-3 rounded-xl px-4 font-medium ${isActive?'bg-emerald-50 text-emerald-700':'text-slate-600 hover:bg-slate-50'}`}>{icon}<span>{label}</span></NavLink>)}</nav></aside><div className="lg:pl-60"><header className="sticky top-0 z-30 flex min-h-14 items-center border-b bg-white/95 px-4 backdrop-blur lg:px-8"><h1 className="text-lg font-semibold">{title||'AI 虚拟公司'}</h1></header><main className="mx-auto max-w-5xl px-3 py-4 pb-24 sm:px-6 lg:py-7 lg:pb-8">{children}</main></div><nav className="fixed inset-x-0 bottom-0 z-50 grid h-[72px] grid-cols-4 border-t bg-white/95 pb-[env(safe-area-inset-bottom)] backdrop-blur lg:hidden">{tabs.map(([icon,label,to])=><NavLink key={to} to={to} className={({isActive})=>`flex flex-col items-center justify-center gap-0.5 text-xs ${isActive?'text-emerald-600':'text-slate-500'}`}><span className="text-xl">{icon}</span><span>{label}</span></NavLink>)}</nav></div>}
