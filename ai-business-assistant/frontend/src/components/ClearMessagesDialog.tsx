import {useState} from 'react';import {clearAllMessages} from '../api/employees';
export default function ClearMessagesDialog({onClose,onDone}:{onClose:()=>void;onDone:()=>void}){const [archive,setArchive]=useState(false);const [busy,setBusy]=useState(false);return <div className="fixed inset-0 z-[80] flex items-end justify-center bg-black/40 p-3 sm:items-center" onClick={onClose}><section onClick={e=>e.stopPropagation()} className="w-full max-w-lg rounded-2xl bg-white p-5 shadow-2xl"><h2 className="text-lg font-bold">确认清空所有聊天消息？</h2><p className="mt-2 text-sm leading-6 text-slate-600">清空后，聊天消息会临时保存在“最近删除”中。你可以在设置中恢复，默认保留 7 天。</p><label className="mt-4 flex items-center gap-3 rounded-xl border p-3"><input type="checkbox" checked={archive} onChange={e=>setArchive(e.target.checked)}/><span>清空后生成工作存档</span></label><div className="mt-5 grid grid-cols-2 gap-3"><button onClick={onClose} className="rounded-xl border">取消</button><button disabled={busy} onClick={async()=>{setBusy(true);await clearAllMessages(archive);onDone()}} className="rounded-xl bg-red-600 text-white">清空所有员工聊天</button></div></section></div>}
