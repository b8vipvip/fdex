export default function UploadBox({ onUpload, loading }: { onUpload: (file: File) => Promise<void> | void; loading?: boolean }) { return <label className={`flex min-h-32 flex-col items-center justify-center rounded-2xl border-2 border-dashed border-blue-300 bg-blue-50/60 px-4 py-5 text-center transition hover:bg-blue-50 sm:min-h-36 ${loading ? 'cursor-wait opacity-70' : 'cursor-pointer'}`}><input disabled={loading} type="file" className="hidden" onChange={e => { const file = e.target.files?.[0]; if (file) void onUpload(file); e.currentTarget.value = ''; }} /><div className="text-2xl">📎</div><div className="mt-1.5 font-medium text-blue-900">{loading ? '正在上传...' : '点击选择资料上传'}</div><p className="mt-1 max-w-2xl text-xs leading-5 text-slate-500 sm:text-sm">支持截图、Excel、PDF、Word、聊天记录、录屏、代码及 .log / .conf / .ini / .yaml 等文本文件，单文件 50MB 内</p></label>; }
