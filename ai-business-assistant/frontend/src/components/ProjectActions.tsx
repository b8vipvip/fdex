import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {getEmployees} from '../api/employees';
export default function ProjectActions({projectId}:{projectId:number}){const [id,setId]=useState<number>();const [open,setOpen]=useState(false);useEffect(()=>{getEmployees().then(x=>setId(x.find(e=>e.is_material_manager)?.id))},[]);return <div className="relative"><button onClick={()=>setOpen(!open)} className="w-11 text-2xl">＋</button>{open&&<div className="absolute right-0 top-12 z-50 w-40 overflow-hidden rounded-xl bg-white shadow-xl">{id?<Link to={`/messages/${id}?project_id=${projectId}`} className="block p-3">上传资料</Link>:<span className="block p-3 text-slate-400">资料员加载中</span>}<a href="#upload" className="block p-3">分析资料</a><a href="#reports" className="block p-3">生成方案</a></div>}</div>}
