import {useEffect,useMemo,useState} from 'react';import {apiClient} from '../api/client';
export default function IndustrySelector({value,onChange}:{value:string;onChange:(v:string)=>void}){const [rows,setRows]=useState<string[]>([]),[q,setQ]=useState('');useEffect(()=>{apiClient.get('/job-roles/industries').then(r=>setRows(r.data))},[]);const filtered=useMemo(()=>rows.filter(x=>x.toLowerCase().includes(q.toLowerCase())),[rows,q]);return <div className="space-y-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索公司行业" className="w-full rounded-xl border px-3"/><select value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-xl border px-3"><option value="">请选择公司默认行业</option>{filtered.map(x=><option key={x}>{x}</option>)}</select></div>}
