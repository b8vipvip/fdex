import { Link } from 'react-router-dom';
import type { Project } from '../types';
import { formatDate, projectStatusClasses, projectStatusLabels, projectTypeLabels, storageModeLabels } from '../utils/display';
export default function ProjectCard({ project }: { project: Project }) { return <Link to={`/projects/${project.id}`} className="group block w-full rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-blue-200 hover:shadow-md"><div className="flex items-start justify-between gap-3"><h3 className="text-lg font-semibold text-slate-900 group-hover:text-blue-700">{project.title}</h3><span className={`shrink-0 rounded-full px-2.5 py-1 text-xs font-medium ring-1 ring-inset ${projectStatusClasses[project.status] ?? 'bg-slate-100 text-slate-700 ring-slate-200'}`}>{projectStatusLabels[project.status] ?? project.status}</span></div><p className="mt-3 line-clamp-3 min-h-[3.75rem] text-sm leading-5 text-slate-600">{project.description || '暂无描述'}</p><div className="mt-4 flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-slate-600">{projectTypeLabels[project.project_type] ?? project.project_type}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-slate-600">{storageModeLabels[project.storage_mode] ?? project.storage_mode}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-slate-600">需求完整度 {Math.round(project.requirement_score)}%</span></div><p className="mt-4 border-t pt-3 text-xs text-slate-400">更新于 {formatDate(project.updated_at)}</p></Link>; }
