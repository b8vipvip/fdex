import {Link} from 'react-router-dom';import type {Employee} from '../types';import {API_BASE_URL} from '../api/client';
export default function EmployeeAvatar({employee,size='md',link=true}:{employee:Employee;size?:'md'|'lg';link?:boolean}){const src=employee.avatar_url?(employee.avatar_url.startsWith('http')?employee.avatar_url:API_BASE_URL.replace(/\/api$/,'')+employee.avatar_url):'';const content=src?<img src={src} alt={`${employee.name}头像`} className="h-full w-full object-cover"/>:employee.avatar;const cls=`flex shrink-0 items-center justify-center overflow-hidden rounded-xl bg-emerald-50 ${size==='lg'?'h-20 w-20 text-4xl':'h-12 w-12 text-2xl'}`;return link?<Link to={`/employees/${employee.id}`} onClick={e=>e.stopPropagation()} className={cls}>{content}</Link>:<span className={cls}>{content}</span>}
