import {Link} from 'react-router-dom';import type {Employee} from '../types';import {API_BASE_URL} from '../api/client';
export default function EmployeeAvatar({employee,size='md'}:{employee:Employee;size?:'md'|'lg'}){const src=employee.avatar_url?(employee.avatar_url.startsWith('http')?employee.avatar_url:API_BASE_URL.replace(/\/api$/,'')+employee.avatar_url):'';return <Link to={`/employees/${employee.id}`} onClick={e=>e.stopPropagation()} className={`flex shrink-0 items-center justify-center overflow-hidden rounded-xl bg-emerald-50 ${size==='lg'?'h-20 w-20 text-4xl':'h-12 w-12 text-2xl'}`}>{src?<img src={src} className="h-full w-full object-cover"/>:employee.avatar}</Link>}
