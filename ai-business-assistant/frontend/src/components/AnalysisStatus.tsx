export default function AnalysisStatus({ status, score }: { status: string; score: number }) { return <div className="rounded-2xl bg-white p-4 shadow-sm"><div className="text-sm text-slate-500">工作状态</div><div className="mt-1 text-xl font-bold">{status}</div><div className="mt-3 h-2 rounded bg-slate-100"><div className="h-2 rounded bg-green-500" style={{ width: `${Math.min(100, score)}%` }} /></div><div className="mt-1 text-xs text-slate-500">工作完整度 {Math.round(score)}%</div></div>; }
