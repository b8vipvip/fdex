import {useEffect,useState} from 'react';import Layout from '../components/Layout';import IndustrySelector from '../components/IndustrySelector';import {apiClient} from '../api/client';import {useAuthStore} from '../store/authStore';
export default function AccountPage(){const [x,setX]=useState<any>();const loadMe=useAuthStore(s=>s.loadMe);useEffect(()=>{apiClient.get('/account/profile').then(r=>setX(r.data))},[]);if(!x)return <Layout title="账号信息" back>加载中…</Layout>;return <Layout title="账号信息" back><div className="mx-auto max-w-xl space-y-4 rounded-2xl bg-white p-4"><label className="block">昵称<input value={x.name} onChange={e=>setX({...x,name:e.target.value})} className="mt-1 w-full rounded-xl border px-3"/></label><label className="block">邮箱<input value={x.email} readOnly className="mt-1 w-full rounded-xl border bg-slate-50 px-3"/></label><label className="block">公司名称<input value={x.company_name} onChange={e=>setX({...x,company_name:e.target.value})} className="mt-1 w-full rounded-xl border px-3"/></label><label className="block space-y-2">公司默认行业<IndustrySelector value={x.company_industry||''} onChange={v=>setX({...x,company_industry:v})}/><small className="text-slate-500">用于员工职位排序和公司自动化运行。</small></label><label className="block">最近删除保留时间<select value={x.deleted_retention_days} onChange={e=>setX({...x,deleted_retention_days:Number(e.target.value)})} className="mt-1 w-full rounded-xl border px-3"><option>7</option><option>15</option><option>30</option></select></label><p>企业实名：{x.is_verified_company?'已实名':'未实名'} · 个人实名：{x.realname_verified?'已实名':'未实名'}</p><button onClick={async()=>{await apiClient.put('/account/profile',x);await loadMe();alert('已保存')}} className="w-full rounded-xl bg-emerald-600 py-3 text-white">保存账号信息</button></div></Layout>}
