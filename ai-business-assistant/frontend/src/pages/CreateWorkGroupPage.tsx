import {FormEvent,useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import Layout from '../components/Layout';import {getEmployees} from '../api/employees';import {getProjects} from '../api/projects';import {createWorkGroup} from '../api/workGroups';import type {Employee,Project} from '../types';
export default function CreateWorkGroupPage(){const [name,setName]=useState(''),[description,setDescription]=useState(''),[workId,setWorkId]=useState(''),[employees,setEmployees]=useState<Employee[]>([]),[projects,setProjects]=useState<Project[]>([]),[ids,setIds]=useState<number[]>([]);const nav=useNavigate();useEffect(()=>{getEmployees().then(es=>{setEmployees(es);setIds(es.filter(e=>e.is_material_manager).map(e=>e.id))});getProjects().then(setProjects)},[]);async function submit(e:FormEvent){e.preventDefault();const g=await createWorkGroup({name,description,work_id:workId?Number(workId):null,employee_ids:ids});nav(`/groups/${g.id}`)}return <Layout title="创建工作群" back><form onSubmit={submit} className="mx-auto max-w-xl space-y-4 rounded-2xl bg-white p-4"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="群名称" className="w-full rounded-xl border px-3"/><select value={workId} onChange={e=>setWorkId(e.target.value)} className="w-full rounded-xl border px-3"><option value="">关联工作（可选）</option>{projects.map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="群描述" className="min-h-24 w-full rounded-xl border p-3"/><div className="flex justify-between"><b>添加员工</b><button type="button" onClick={()=>setIds(ids.length===employees.length?[]:employees.map(e=>e.id))} className="text-emerald-600">{ids.length===employees.length?'取消全选':'全选'}</button></div><div className="max-h-72 space-y-2 overflow-auto">{employees.map(e=><label key={e.id} className="flex items-center gap-3 rounded-xl border p-3"><input type="checkbox" checked={ids.includes(e.id)} onChange={()=>setIds(v=>v.includes(e.id)?v.filter(x=>x!==e.id):[...v,e.id])}/><span>{e.avatar}</span><span className="flex-1">{e.name} · {e.position}</span>{e.is_material_manager&&<small>默认加入</small>}</label>)}</div><button className="w-full rounded-xl bg-emerald-600 py-3 font-semibold text-white">创建并进入群聊</button></form></Layout>}
