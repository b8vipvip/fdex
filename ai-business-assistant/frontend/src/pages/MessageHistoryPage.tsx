import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router-dom';import Layout from '../components/Layout';import {searchMessages} from '../api/employees';import type {EmployeeMessage} from '../types';
export default function MessageHistoryPage(){const id=Number(useParams().employeeId);const [q,setQ]=useState('');const [date,setDate]=useState('');const [rows,setRows]=useState<EmployeeMessage[]>([]);useEffect(()=>{searchMessages(id,q,date).then(setRows)},[id,q,date]);return <Layout title="聊天记录" back hideTabs><div className="mb-4 flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索聊天记录" className="min-w-0 flex-1 rounded-xl border px-3"/><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="rounded-xl border px-2"/></div><div className="space-y-3">{rows.map(x=><Link to={`/messages/${id}?message=${x.id}`} key={x.id} className="block rounded-2xl bg-white p-4"><p className="text-xs text-slate-400">{new Date(x.created_at).toLocaleString()}</p><p className="mt-1 line-clamp-3">{x.content}</p></Link>)}</div></Layout>}
