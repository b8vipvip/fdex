import { FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Layout from '../components/Layout';
import { createProject } from '../api/projects';
import type { ProfessionalLevel } from '../types';
export default function ProjectCreatePage(){const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [professional_level,setLevel]=useState<ProfessionalLevel>('business');const nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();const p=await createProject({title,description,professional_level});nav(`/projects/${p.id}`)}return <Layout><div className="mx-auto max-w-2xl rounded-3xl bg-white p-6 shadow-sm"><h1 className="text-2xl font-bold">新建项目</h1><form onSubmit={submit} className="mt-6 space-y-4"><input className="w-full rounded-xl border p-3" placeholder="项目标题" value={title} onChange={e=>setTitle(e.target.value)} /><textarea className="min-h-40 w-full rounded-xl border p-3" placeholder="用大白话描述需求：你想解决什么问题？现在怎么做？希望 AI 或系统帮你做什么？" value={description} onChange={e=>setDescription(e.target.value)} /><select className="w-full rounded-xl border p-3" value={professional_level} onChange={e=>setLevel(e.target.value as ProfessionalLevel)}><option value="beginner">完全小白</option><option value="business">懂业务不懂技术</option><option value="product">产品/项目经理</option><option value="developer">技术人员</option><option value="auto">AI自动判断</option></select><button className="rounded-xl bg-blue-600 px-5 py-3 text-white">创建项目</button></form></div></Layout>}
