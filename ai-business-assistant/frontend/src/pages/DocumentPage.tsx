import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import Layout from '../components/Layout';
import DocumentViewer from '../components/DocumentViewer';
import { exportReportUrl, getReport } from '../api/documents';
import type { Report } from '../types';
export default function DocumentPage(){const id=Number(useParams().id);const [report,setReport]=useState<Report>();const [downloading,setDownloading]=useState(false);useEffect(()=>{getReport(id).then(setReport)},[id]);async function copy(){if(report) await navigator.clipboard.writeText(report.content_markdown);}async function download(){const token=localStorage.getItem('token');setDownloading(true);try{const response=await fetch(exportReportUrl(id),{headers:token?{Authorization:`Bearer ${token}`}:{}});if(!response.ok) throw new Error('Markdown 导出失败');const blob=await response.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${report?.report_type||'report'}.md`;a.click();URL.revokeObjectURL(url);}finally{setDownloading(false);}}return <Layout>{report&&<><div className="mb-4 flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h1 className="text-2xl font-bold">{report.title}</h1><p className="text-sm text-slate-500">Markdown 文档，可复制或导出</p></div><div className="flex gap-2"><button onClick={copy} className="rounded-xl bg-slate-900 px-4 py-2 text-white">复制内容</button><button disabled={downloading} onClick={download} className="rounded-xl bg-blue-600 px-4 py-2 text-white disabled:opacity-60">{downloading?'导出中...':'导出 Markdown'}</button></div></div><DocumentViewer report={report}/></>}</Layout>}
